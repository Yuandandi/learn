// appened data to the end of an array with push function

var ourArray = (["danzor", "human", "read"]);
// ourArray.push("foo"); // single array push
ourArray.push(["haha", "hihi"]); // push another arrays inside an array

console.log(ourArray);

// the default stdout is always Using single quotes
