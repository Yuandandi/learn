// strings are immutable ; can't be altered once created
// doesn't mean can't be changed (It has to redefine)

var myStr = "Jello World";

// myStr[0] = "H" // It still print "Jello World"

myStr = "Hello World" // type the whole thing instead
console.log(myStr);
