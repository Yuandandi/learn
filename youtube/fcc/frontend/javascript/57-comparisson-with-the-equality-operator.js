// the equallity operator
function  testEqual(val) {
  if (val == 12) { // single equal sign means set variable, but double equal sign == means check if its equal or not
    return "Equal";
  }
  return "Not Equal";
}

console.log(testEqual(10));
