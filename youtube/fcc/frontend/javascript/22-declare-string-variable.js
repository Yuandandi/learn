// string literals ('' "" `` ) is a string

var firstName = "Alan";
var lastName = " Turing"; // space to devide each word

console.log(firstName + lastName);

var firstName = 'Alan';
var lastName = " Turing"; // space to devide each word

console.log(firstName + lastName);

var firstName = 'Alan';
var lastName = ` Turing`; // space to devide each word

console.log(firstName + lastName);

// all still works well
