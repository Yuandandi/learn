// document.getElementById("count").innerText = 5

let count = 0 // store the data inside the variable

console.log(count) // put count variable inside print statement in console


// 1. Create a variable, myAge, and set its value to your age

// console.log(myAge); // JavaScript read from above to the bottom, You need to initialize first by store into a variable
let myAge = 22

// 2. Log the myAge variable to the console

console.log(myAge);
