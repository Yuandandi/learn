// parseint Function
// It takes a string and returns an integer
// if can't be converted into an integer It returns in NaN for not a number.

function convertToInteger(str) {
  return parseInt(str);
}

console.log(convertToInteger("56")); // 56 (integer)
