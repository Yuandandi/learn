// PARAMETERS

// are the variable that add it's place holder for the value to be input to a function when it's called

function  ourFunctionWithArgs(a, b) {
  console.log(a - b);
}

ourFunctionWithArgs(10, 5); // we pass this data into the function

function plus(a, b) {
  console.log(a + b);
}

plus(10, 5);
