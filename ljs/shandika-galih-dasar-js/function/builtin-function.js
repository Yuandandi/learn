// {{{ Function Definition

// sub-program or sub-routine that can be called or "invoked" in another part of the program
// structur maker of JavaScript building
// procedure (collection of 'statement' to do task);
// function declaration -> invoked function;
// part of first-class Object;

// }}}
// {{{ why we use function?

// reusability (DRY: do not repeat yourself)
// decomposition/abstraction (dipecah menjadi beberapa bagian)
// modularity;

// }}};
// {{{ Function Category

// Built-in Function 
// function that available by JavaScript
// to use, just invoke the function name
// e.g. alert(), confirm(), prompt(); // sample of builtin window Object

// User-Defined Function
// Function that we (programmer) made
// Using keyword Function
// Function name (optional)
// parameter/Argument; (placed inside (), pass 0 or more parameter, separate by ',')
// Function body wrap by {  }
// can return value or not return

// }}};
// {{{ Built-in Function : String

// charAt(), slice(), splice(), spliit(), toString(), toLowerCase(), toUpperCase();

// }}}
// {{{ Built-in Function : Math

// sin(), cos(), tan(), randon(), round(), floor(), ceil(), log();

// }}};
// {{{ charAt()

let nama = "joko anwar";

// JavaScript is 0 indexing;
console.log(nama.charAt(3)); // o

// }}}
// {{{ random()

let ran = Math.random();
console.log(ran); // random decimal number between 0 - 1, except both

// }}};
